{
  "title": "{{ message.commonLabels.alertname }}",
  "body": "{{ message.commonAnnotations.summary }}",
  "tags": "{% for key, value in message.commonLabels.items() %}{{ key }}={{ value }}{% if not loop.last %},{% endif %}{% endfor %}"
}
